{
  "name": "prompt-maker-gas",
  "version": "1.0.0",
  "description": "Visual Prompt Maker — Google Apps Script Web App (Vue 3 + Vite)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "wrap": "node scripts/wrap.js",
    "push": "clasp push",
    "deploy:dev": "npm run build && npm run wrap && clasp push",
    "deploy": "npm run build && npm run wrap && clasp push && clasp deploy",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.2.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
